
File junitfile = new File(basedir, "junit.xml");
if (!junitfile.exists()) {
    throw new FileNotFoundException("Could not find file: " + junitfile);
}

File jenkinsFile = new File(basedir, "lightning-jenkins.properties");
if (!jenkinsFile.exists()) {
    throw new FileNotFoundException("Could not find file: " + jenkinsFile);
}
